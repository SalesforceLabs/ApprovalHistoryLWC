import { VNode } from '../../3rdparty/snabbdom/types';
declare function createContext(vnode: VNode): void;
declare const contextModule: {
    create: typeof createContext;
};
export default contextModule;
