import { VNode } from '../3rdparty/snabbdom/types';
import { VM } from './vm';
/**
 * Function producing style based on a host and a shadow selector. This function is invoked by
 * the engine with different values depending on the mode that the component is running on.
 */
export declare type StylesheetFactory = (hostSelector: string, shadowSelector: string, nativeShadow: boolean) => string;
/**
 * Reset the styling token applied to the host element.
 */
export declare function resetStyleAttributes(vm: VM): void;
/**
 * Apply/Update the styling token applied to the host element.
 */
export declare function applyStyleAttributes(vm: VM, hostAttribute: string, shadowAttribute: string): void;
export declare function evaluateCSS(vm: VM, stylesheets: StylesheetFactory[], hostAttribute: string, shadowAttribute: string): VNode | null;
