"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
/*
 * Copyright (c) 2018, salesforce.com, inc.
 * All rights reserved.
 * SPDX-License-Identifier: MIT
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/MIT
 */
const t = __importStar(require("@babel/types"));
const helpers_1 = require("../helpers");
const constants_1 = require("../../shared/constants");
function moduleNameToLookup(name) {
    const localIdentifier = helpers_1.identifierFromComponentName(name);
    return t.variableDeclaration('const', [
        t.variableDeclarator(localIdentifier, t.memberExpression(t.identifier(constants_1.TEMPLATE_MODULES_PARAMETER), t.stringLiteral(name), true)),
    ]);
}
function format(templateFn, state) {
    const lookups = state.dependencies.map(cmpClassName => moduleNameToLookup(cmpClassName));
    const metadata = helpers_1.generateTemplateMetadata(state);
    return t.program([
        ...lookups,
        templateFn,
        ...metadata,
        t.returnStatement(t.identifier(constants_1.TEMPLATE_FUNCTION_NAME)),
    ]);
}
exports.format = format;
//# sourceMappingURL=function.js.map