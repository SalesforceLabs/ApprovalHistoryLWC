import { CompilerDiagnostic } from '@lwc/errors';
import { NormalizedCompileOptions } from '../options';
import { SourceMap } from '../compiler/compiler';
export interface BundleReport {
    code: string;
    diagnostics: CompilerDiagnostic[];
    map: SourceMap | null;
}
export declare function bundle(options: NormalizedCompileOptions): Promise<BundleReport>;
