import { CompilerDiagnostic } from '@lwc/errors';
import { CompileOptions, NormalizedOutputConfig } from '../options';
export { default as templateCompiler } from '@lwc/template-compiler';
export interface CompilerOutput {
    success: boolean;
    diagnostics: CompilerDiagnostic[];
    result?: BundleResult;
    version: string;
}
export interface BundleResult {
    code: string;
    map: SourceMap | null;
    outputConfig: NormalizedOutputConfig;
}
export declare type SourceMap = any;
export declare function compile(options: CompileOptions): Promise<CompilerOutput>;
