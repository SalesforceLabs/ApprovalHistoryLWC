import { NormalizedTransformOptions, TransformOptions } from '../options';
import { SourceMap } from '../compiler/compiler';
export interface FileTransformerResult {
    code: string;
    map: SourceMap | null;
}
/**
 * Transforms the passed code. Returning a Promise of an object with the generated code, source map
 * and gathered metadata.
 *
 * @deprecated Use transformSync instead.
 */
export declare function transform(src: string, filename: string, options: TransformOptions): Promise<FileTransformerResult>;
/**
 * Transform the passed source code. Returning an object with the generated code, source map and
 * gathered metadata.
 */
export declare function transformSync(src: string, filename: string, options: TransformOptions): FileTransformerResult;
export declare function transformFile(src: string, filename: string, options: NormalizedTransformOptions): FileTransformerResult;
